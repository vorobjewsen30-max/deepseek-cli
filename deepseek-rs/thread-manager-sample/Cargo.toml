[package]
name = "deepseek-thread-manager-sample"
version.workspace = true
edition.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
anyhow = { workspace = true }
clap = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
# Keep this sample limited to a single DeepSeek workspace dependency.
# Add new DeepSeek surface area to `deepseek-core-api` instead of depending on
# additional `codex-*` crates here.
deepseek-core-api = { workspace = true }
tracing = { workspace = true }
