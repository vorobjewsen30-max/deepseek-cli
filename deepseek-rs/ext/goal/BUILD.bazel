load("//:defs.bzl", "codex_rust_crate")

codex_rust_crate(
    name = "goal",
    compile_data = glob([
        "templates/**",
    ]),
    crate_name = "deepseek_goal_extension",
    integration_compile_data_extra = ["src/accounting.rs"],
)
